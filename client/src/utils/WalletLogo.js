import { Wallets } from "./constants";

export const GetWalletLogo=(name)=>{
    switch (name) {
        case Wallets.Injected:
            return 
            break;
    
        default:
            break;
    }
}